---
topic: Child-local BMad workflow and artifact preservation
goal: Run all BMad workflows from the DayOne child project while preserving project-owned configuration and artifacts
baseline: feature/bench after Story 1.3 is merged
updated: 2026-07-27T11:32
---

- (direction) Target the child repository as the BMad project root; workflows must resolve configuration, scripts, knowledge, planning artifacts, implementation artifacts, and memory from the child.
- (decision) Keep team BMad configuration and customizations committed in the child; keep personal config user files gitignored.
- (constraint) Story 1.3 must be merged into feature/bench before implementation begins; the implementation prompt must inspect the latest code and avoid assuming the pre-merge tree.
- (constraint) Preserve the distinction between environment-installed skills and project-owned BMad runtime/configuration; do not vendor credentials or unrelated hub state.
- (direction) All durable planning, story, sprint, spec, and workflow memory artifacts required by BMad must live under the child repository and be committed when team-owned.
- (assumption) The initial child-local setup may use the currently installed BMad runtime as its source, while the child stores the project integration contract and can document any external skill-install prerequisite.
- (event) Spec drafted for later execution after Story 1.3 is merged and feature/bench is the principal baseline.
- (event) Coherence validation passed: capabilities have intent and success, capability IDs are unique, constraints are decision-bending, non-goals are explicit, and the success signal is testable.
- (event) Preservation validation passed: child instructions, development workflow, artifact locations, post-Story 1.3 feature/bench baseline, backend boundary, and credential safety were retained in the kernel or companions.
- (event) Spec finalized as a future implementation contract; execute only after Story 1.3 is merged and revalidate against the latest feature/bench code.
