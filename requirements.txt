# NOTE: kept for pip compatibility; the source of truth is pyproject.toml (use `uv sync --dev`).
# Core workshop dependencies
PyYAML>=6.0.2
pydantic>=2.8.0

# Bench domain daily cycle (local-only, no AWS required) — see docs/adr/0002
langgraph>=0.2
python-dotenv>=1.0

# Uncomment when running with the real Strands SDK / AgentCore environment.
# strands-agents
# bedrock-agentcore
# bedrock-agentcore-starter-toolkit

# Dev/test
pytest>=8.0.0
pypdf>=4.0
